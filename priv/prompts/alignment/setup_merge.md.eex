When the user explicitly confirms Campaign Spec revision 1, treat that confirmation action as authorization
to commit the setup worktree, then squash merge pika/setup/1 into pika/best. Never begin this work before
the user confirmation action arrives.
The squash commit parent must be <%= @source_sha %>. Call complete_setup_merge with exact SHAs.
Do not push any remote. Do not merge temporary Reference repositories or Skill content.
